# ============================================
# FMS (File Management System) Configuration
# ============================================

# --------------------------------------------
# API Configuration
# --------------------------------------------
# Backend API base URL
VITE_API_URL=http://localhost:3000/api

# --------------------------------------------
# Application Configuration
# --------------------------------------------
# Application name displayed in UI
VITE_APP_NAME=FMS

# Maximum file upload size in bytes (default: 100MB)
VITE_MAX_FILE_SIZE=104857600

# Environment (development, staging, production)
VITE_ENVIRONMENT=development

# --------------------------------------------
# AWS Cognito Configuration
# --------------------------------------------
# Get these values from AWS Cognito Console
# Navigate to: AWS Console > Cognito > User Pools

# User Pool ID (format: us-east-1_XXXXXXXXX)
VITE_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX

# App Client ID (found in App Integration tab)
VITE_COGNITO_CLIENT_ID=your-client-id-here

# AWS Region where your Cognito User Pool is located
VITE_COGNITO_REGION=us-east-1

# Optional: Identity Pool ID (for AWS resource access)
# VITE_COGNITO_IDENTITY_POOL_ID=us-east-1:xxxx-xxxx-xxxx-xxxx

# --------------------------------------------
# OTP Configuration
# --------------------------------------------
# Length of OTP code (default: 6 digits)
VITE_OTP_LENGTH=6

# OTP expiry time in minutes (informational, actual expiry set in Cognito)
# VITE_OTP_EXPIRY_MINUTES=5

# --------------------------------------------
# Storage Configuration
# --------------------------------------------
# Default storage quota for new users in bytes (default: 10GB)
# VITE_DEFAULT_STORAGE_QUOTA=10737418240

# Minimum storage quota in bytes (default: 1GB)
# VITE_MIN_STORAGE_QUOTA=1073741824

# Maximum storage quota in bytes (default: 1TB)
# VITE_MAX_STORAGE_QUOTA=1099511627776

# --------------------------------------------
# Feature Flags (Optional)
# --------------------------------------------
# Enable/disable features
# VITE_ENABLE_MFA=true
# VITE_ENABLE_SOCIAL_LOGIN=false
# VITE_ENABLE_FILE_SHARING=true
# VITE_ENABLE_ANALYTICS=false

# --------------------------------------------
# UI Configuration (Optional)
# --------------------------------------------
# Default theme (light, dark)
# VITE_DEFAULT_THEME=light

# Default view mode for files (grid, list)
# VITE_DEFAULT_VIEW_MODE=grid

# Items per page for pagination
# VITE_ITEMS_PER_PAGE=20

# --------------------------------------------
# Development Configuration (Optional)
# --------------------------------------------
# Enable debug mode
# VITE_DEBUG=false

# Enable mock data for development
# VITE_USE_MOCK_DATA=false

# API request timeout in milliseconds
# VITE_API_TIMEOUT=30000

# --------------------------------------------
# Notes
# --------------------------------------------
# 1. Copy this file to .env.development for local development
# 2. Never commit .env.development or .env.production to version control
# 3. Update VITE_COGNITO_* values after setting up AWS Cognito
# 4. All VITE_* variables are exposed to the client-side code
# 5. Sensitive data should be handled by the backend API