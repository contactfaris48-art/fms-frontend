#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-push checks..."
echo ""

# Run build and secret detection
echo "📦 Building and checking for secrets..."
pnpm build:check

if [ $? -ne 0 ]; then
  echo ""
  echo "❌ Pre-push check failed!"
  echo "   Secrets were detected in the production build."
  echo "   Please remove all secrets before pushing."
  echo ""
  exit 1
fi

echo ""
echo "✅ Pre-push checks passed!"
echo ""