/**
 * Centralized error handling utilities
 * Export all error-related types and handlers
 */
export { ErrorHandler } from './errorHandler';
export { ErrorType } from './types';
export type { AppError, RTKQueryError, ErrorContext } from './types';